{
  "name": "slack-bot",
  "version": "1.0.0",
  "description": "Simple bot for slack",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "DucLA",
  "license": "ISC",
  "dependencies": {
    "@slack/bolt": "^3.13.0",
    "dotenv": "^16.0.3",
    "openai": "^3.2.1"
  }
}
